<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Quotes</title>
    <link href="css/style.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/e8a278aa48.js" crossorigin="anonymous"></script>
    <script
    src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
    crossorigin="anonymous"></script>
    <script src ="js/like.js"></script>
</head>

<body>  

    <a class="newPost" href="/create">Write a new post</a>

    <% for(let i=0; i<rows.length; i++){%>

    <div class="frame">
        <header class="book_header">
            <div class="author">
                <span class="description">Author</span>
                <span class="authorName capsmallfont"><%= rows[i].author %></span></div>
            <div class="date">
                <span class="description">Date</span>
                <span class="date capsmallfont"><%= rows[i].date %></span></div>
        </header>
        <h1 class="title"><%= rows[i].title %></h1>
        <section class="book_body">
          <pre><%= rows[i].contents %></pre>
        </section>
        <section class="like">
			<% if(rows[i].like==1){%>
				<i class="fas fa-heart" data-like="0" data-postid="<%=rows[i].id%>"></i>
			<%} else {%>
				<i class="far fa-heart" data-like="1" data-postid="<%=rows[i].id%>"></i>
			<%}%>
        </section>
        <a class="edit" href= '/update?id=<%=i%>'>update</a><a class="delete" href="/delete?id=<%=i%>">delete</a>
    </div>
  <%}%>

</body>

</html>